import {BRAND_NAME} from "../../lib/config";
import {SectionPageLayout} from "../../components/layouts/SectionPageLayout"; import {siteSubMenu} from "./index.mdx";
export const meta = {
  title: `${BRAND_NAME} - Collecting For Others`,
}

# Collecting for Others

1. Ask them to Log in to SingPass
2. Check their Eligibility
3. Choose a timeslot
4. Assign yourself to help them

export default ({children}) => <SectionPageLayout sideNavigationMenu={siteSubMenu}
                                                  meta={meta}>{children}</SectionPageLayout>
